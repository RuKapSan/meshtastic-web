# Database configuration
DATABASE_PATH=/app/backend/data/meshradar.db

# USB Device Configuration (optional)
# Only needed if you want to use USB/Serial connection in Docker
# Uncomment the devices section in docker-compose.yml and set your device path:
# Common paths:
# - /dev/ttyUSB0    (Linux)
# - /dev/ttyACM0    (Linux, some boards)
# - COM3             (Windows with WSL)
